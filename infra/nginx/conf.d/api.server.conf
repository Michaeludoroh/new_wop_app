# API — api.example.com (or api.localhost for compose testing)
server {
  listen 80;
  listen [::]:80;
  server_name api.example.com api.localhost;

  # Uncomment after placing TLS certs in infra/nginx/certs/
  # return 301 https://$host$request_uri;

  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "DENY" always;
  add_header Referrer-Policy "strict-origin-when-cross-origin" always;
  add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;

  client_max_body_size 100m;

  location / {
    proxy_pass http://ministry_api;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Correlation-Id $http_x_correlation_id;
    proxy_read_timeout 120s;
    proxy_connect_timeout 10s;
  }
}

# HTTPS — enable when certs are present (see infra/nginx/certs/README.md)
# server {
#   listen 443 ssl http2;
#   listen [::]:443 ssl http2;
#   server_name api.example.com;
#
#   ssl_certificate     /etc/nginx/certs/fullchain.pem;
#   ssl_certificate_key /etc/nginx/certs/privkey.pem;
#   ssl_protocols       TLSv1.2 TLSv1.3;
#   ssl_ciphers         HIGH:!aNULL:!MD5;
#   ssl_prefer_server_ciphers on;
#
#   add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#   add_header X-Content-Type-Options "nosniff" always;
#   add_header X-Frame-Options "DENY" always;
#
#   client_max_body_size 100m;
#
#   location / {
#     proxy_pass http://ministry_api;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header X-Correlation-Id $http_x_correlation_id;
#   }
# }
