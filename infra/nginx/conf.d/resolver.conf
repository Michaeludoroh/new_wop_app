# Docker Compose embedded DNS — defer hostname resolution until request time.
# 127.0.0.11 is the resolver address inside every Docker container network namespace.
resolver 127.0.0.11 valid=10s ipv6=off;
resolver_timeout 5s;
