This imageset is unused. LaunchScreen.storyboard uses a branded solid-color splash
(with the WOPP label) and does not reference LaunchImage.

Keep this directory only so the Assets.xcassets catalog remains valid.
