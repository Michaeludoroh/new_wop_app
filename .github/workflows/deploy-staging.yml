name: Deploy Staging

on:
  push:
    branches: ["develop"]
  workflow_dispatch:

concurrency:
  group: deploy-staging
  cancel-in-progress: false

jobs:
  deploy:
    uses: ./.github/workflows/_deploy-reusable.yml
    with:
      environment_name: staging
      image_tag: staging-${{ github.sha }}
      api_health_url: https://staging-api.example.com/api/v1/health
      ws_health_url: https://staging-ws.example.com
      admin_health_url: https://staging-admin.example.com
      run_compose_up: false
    secrets:
      DATABASE_URL: ${{ secrets.STAGING_DATABASE_URL }}
      REDIS_URL: ${{ secrets.STAGING_REDIS_URL }}
      JWT_ACCESS_SECRET: ${{ secrets.STAGING_JWT_ACCESS_SECRET }}
      JWT_REFRESH_SECRET: ${{ secrets.STAGING_JWT_REFRESH_SECRET }}
      CONTENT_ACCESS_SECRET: ${{ secrets.STAGING_CONTENT_ACCESS_SECRET }}
      METRICS_AUTH_TOKEN: ${{ secrets.STAGING_METRICS_AUTH_TOKEN }}
      CORS_ORIGIN: ${{ secrets.STAGING_CORS_ORIGIN }}
      API_PUBLIC_URL: ${{ secrets.STAGING_API_PUBLIC_URL }}
